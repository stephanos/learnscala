package com.crashnote.test.unit

/*
package com.crashnote.test.unit

import org.specs2.mutable.Specification
import org.specs2.matcher.DataTables

class TestSpec extends Specification with DataTables {

    "Test Util" should {

        val data1 = "test string 1"
        val data2 = "test string 2"

        "do test" >> {
            "data" | "data2" |
                    data1 !! data2 |
                    data1 !! data2 |> {
                (d1, d2) => 1 === 0
            }
        }

        for (e <- List(data1,data2)) {
            "must match last " + e >> {
                1 === 0
            }
        }
    }
}*/
